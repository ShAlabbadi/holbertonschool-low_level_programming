#include "main.h"
/**
* mul - function that multiplies two integers.
* @a : the first integers.
* @b : the second integers.
* Return: the function return the multiplication ot the two integers.
*/
int mul(int a, int b)
{
return (a * b);
}
